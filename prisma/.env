# This dummy environment variable is specifically to fix Vercel's build step.
# Vercel's postinstall script requires this variable to exist to generate the Prisma Client, 
# but the real value is not injected until runtime. Vercel will override this dummy value in production.
POSTGRES_URL="postgresql://postgres:dummy@localhost:5432/dummy"
